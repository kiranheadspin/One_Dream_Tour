import Link from "next/link";
import { CheckCircle2, MessageCircle } from "lucide-react";
import { TOURNAMENT, whatsappUrl } from "@/lib/constants";

export default async function SuccessPage({ searchParams }: { searchParams: Promise<{ reference?: string }> }) {
  const { reference = "Pending" } = await searchParams;
  return <section className="min-h-[780px] bg-[#f7f3ea] px-4 pb-20 pt-40"><div className="mx-auto max-w-2xl border border-[#d7aa54]/40 bg-white p-8 text-center shadow-sm sm:p-12"><CheckCircle2 aria-hidden="true" className="mx-auto size-14 text-[#313999]"/><p className="eyebrow mt-7 text-[#8d672c]">Enquiry received</p><h1 className="mt-3 text-4xl text-[#081326] sm:text-5xl">We’ll take it from here.</h1><p className="mx-auto mt-5 max-w-lg leading-7 text-slate-600">Our operations team will review the details and contact you about eligibility, city availability and the registration invitation.</p><div className="mx-auto mt-8 max-w-sm border-y border-slate-200 py-5"><p className="text-xs uppercase tracking-wider text-slate-500">Your reference</p><p className="mt-2 font-mono text-xl font-bold text-[#081326]">{reference}</p></div><div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row"><a href={whatsappUrl(`Hello One Dream Cup team, my enquiry reference is ${reference}.`)} className="inline-flex h-11 items-center justify-center gap-2 rounded-md bg-[#313999] px-5 text-sm font-bold text-white"><MessageCircle aria-hidden="true" className="size-4"/>Continue on WhatsApp</a><Link href="/" className="inline-flex h-11 items-center justify-center rounded-md border px-5 text-sm font-semibold">Back to homepage</Link></div><p className="mt-7 text-xs text-slate-500">WhatsApp: +91 {TOURNAMENT.whatsapp}. Keep your reference handy.</p></div></section>;
}
