import { AdminHeading } from "@/components/admin/admin-heading";

export default function AdminContentPage(){const fields=["Exact city dates and deadlines","Venues and addresses","Final squad and eligibility rules","Goa finals schedule","Sponsor inventory and deliverables","Announcements and public policy versions"];return <div className="mx-auto max-w-5xl"><AdminHeading eyebrow="Publishing controls" title="Content" description="Unverified operational claims stay unpublished until an administrator supplies and approves them."/><div className="mt-7 grid gap-3">{fields.map(field=><div key={field} className="flex items-center justify-between border bg-white p-5"><div><h2 className="font-sans text-sm font-semibold text-[#081326]">{field}</h2><p className="mt-1 text-xs text-slate-500">Not published · source confirmation required</p></div><span className="rounded-full bg-amber-100 px-3 py-1 text-xs font-semibold text-amber-800">Pending</span></div>)}</div></div>}
