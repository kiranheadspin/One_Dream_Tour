import { AdminHeading } from "@/components/admin/admin-heading";

export default function AdminSettingsPage(){return <div className="mx-auto max-w-5xl"><AdminHeading eyebrow="Configuration" title="Settings" description="Operational values are database-controlled rather than hard-coded into public claims."/><section className="mt-7 border bg-white p-7"><h2 className="text-2xl text-[#081326]">Commercial readiness gate</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-600">Production payment submission stays blocked until `payments_enabled` is explicitly true, the organizer&apos;s UPI ID and phone are configured, and confirmed GST treatment, payment schedule and refund terms are published. Provider-backed checkout can be connected later without changing the payment record or admin review model.</p><div className="mt-6 rounded-md border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">Current demo value: manual UPI review is enabled with non-payable preview details.</div></section></div>}
