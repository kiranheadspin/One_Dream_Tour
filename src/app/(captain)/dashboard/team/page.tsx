import { requireRole } from "@/lib/auth";
import { getCaptainTeam } from "@/lib/teams";
import { AddPlayerDialog } from "@/components/captain/add-player-dialog";
import { Badge } from "@/components/ui/badge";

export default async function TeamPage(){const session=await requireRole("captain");const team=await getCaptainTeam(session.userId);return <div className="mx-auto max-w-6xl"><p className="eyebrow text-[#8d672c]">Registration step 2</p><div className="mt-2 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 className="text-4xl text-[#081326]">Team & players</h1><p className="mt-2 text-sm text-slate-600">Keep company-verified player details current.</p></div><AddPlayerDialog/></div><section className="mt-8 border bg-white"><div className="grid gap-5 border-b p-6 sm:grid-cols-3"><div><p className="text-xs text-slate-500">Team</p><p className="mt-1 font-semibold">{team.name}</p></div><div><p className="text-xs text-slate-500">Company</p><p className="mt-1 font-semibold">{team.company}</p></div><div><p className="text-xs text-slate-500">City</p><p className="mt-1 font-semibold">{team.city}</p></div></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-6 py-3">Player</th><th className="px-6 py-3">Employee ID</th><th className="px-6 py-3">Phone</th><th className="px-6 py-3">Shirt</th><th className="px-6 py-3">Status</th></tr></thead><tbody>{team.players.map(player=><tr key={player.id} className="border-t"><td className="px-6 py-4"><p className="font-semibold">{player.name}{player.isCaptain&&<span className="ml-2 text-xs text-[#8d672c]">Captain</span>}</p><p className="mt-1 text-xs text-slate-500">{player.email}</p></td><td className="px-6 py-4">{player.employeeId}</td><td className="px-6 py-4">{player.phone}</td><td className="px-6 py-4">{player.shirtSize}</td><td className="px-6 py-4"><Badge variant="secondary">{player.status}</Badge></td></tr>)}</tbody></table></div></section></div>}
